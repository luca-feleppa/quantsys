# ROADMAP — Vol Book v2 · IT · EN

> 🇮🇹 Backlog architetturale e strategico emerso dall'audit anti-overfit + analisi book futures/opzioni (sessione 2026-07-07). Ogni item segue il protocollo sperimentale di `CLAUDE.md` (gate pre-registrato in `STATUS.md`, val-first, env-flag inerte, esito scritto anche se negativo). **File SNELLITO 2026-07-16:** contiene solo gli item **aperti** — gli item chiusi (§0 verdetti anti-overfit, A2 FAIL, A5 FAIL, B1 KILL) e quelli applicati e documentati altrove (A6 exec-diag → `AVVIO.md`/`scripts/README.md`; A11 attribution → `scripts/README.md`) vivono in `STATUS.md` con gli esiti completi. Gli ID originali (A1-A14, B2-B3) sono preservati: STATUS e memoria li citano. Piano operativo post-gate-v1: `POST_GATE_V1.md` (root).
> **EN** Architectural and strategic backlog from the anti-overfit audit + futures/options book analysis (2026-07-07 session). Every item follows the experimental protocol in `CLAUDE.md` (pre-registered gate in `STATUS.md`, val-first, inert env-flag, outcome written even if negative). **File SLIMMED 2026-07-16:** open items only — closed items (§0 anti-overfit verdicts, A2 FAIL, A5 FAIL, B1 KILL) and items applied and documented elsewhere (A6 exec-diag → `AVVIO.md`/`scripts/README.md`; A11 attribution → `scripts/README.md`) live in `STATUS.md` with full outcomes. Original IDs (A1-A14, B2-B3) are preserved: STATUS and memory cite them. Post-gate-v1 operating plan: `POST_GATE_V1.md` (root).

---

## A · Miglioramenti modello/infra (priorità = prior × effort⁻¹) · Model/infra improvements

### A1 🔴 Delta-hedge del book opzioni col perpetual Deribit · Options-book delta-hedge via Deribit perpetual
🇮🇹 **Il singolo upgrade più giustificato del progetto** — vedi sezione B. Effort M. **Post-gate n≥20** (non toccare `04b` a campione aperto). Codice pronto e inerte (`04b --hedge`, dettaglio in `docs/MODEL_IMPROVEMENTS.md`); band/conv da congelare col dry-run A6 alla chiusura del gate v1.
**EN** The single most justified upgrade — see section B. Effort M. **Post-gate n≥20** (do not touch `04b` mid-sample). Code ready and inert (`04b --hedge`, detail in `docs/MODEL_IMPROVEMENTS.md`); band/conv to freeze via the A6 dry-run at v1-gate closure.

### A3 🟠 Mixture-of-universes (design B2 in memoria) — prior RIALZATO · prior RAISED
🇮🇹 Backbone condiviso + 3 teste-regime, soft-gate causale su `regime_prob_0/1/2` (già persistite in `data/regime_probs.parquet`). Era accantonato ("migliora σ, non la direzione"); l'audit 2026-06-26 ha stabilito che l'edge short-vol è **Trending-driven** → la calibrazione σ regime-condizionata è ora direttamente monetizzabile. Forward contract invariato. Effort M. Design: memoria `mixture_of_universes_design`. **Stato: IMPLEMENTATO-INERTE 2026-07-12, mai addestrato; gate QLIKE pre-registrato in STATUS il 2026-07-14** (run nella finestra GPU post-gate-v1; prerequisito residuo P3 = audit causality-auditor sui file A3). Dettaglio implementativo: `docs/MODEL_IMPROVEMENTS.md` (sezione A3).
**EN** Shared backbone + 3 regime heads, causal soft-gate on `regime_prob_0/1/2`. Was shelved ("improves σ, not direction"); the 2026-06-26 audit showed the short-vol edge is **Trending-driven** → regime-conditional σ calibration is now directly monetizable. Forward contract unchanged. Effort M. **Status: IMPLEMENTED-INERT 2026-07-12, never trained; QLIKE gate pre-registered in STATUS on 2026-07-14** (run in the post-v1-gate GPU window; residual prerequisite P3 = causality-auditor audit of the A3 files). Implementation detail: `docs/MODEL_IMPROVEMENTS.md` (A3 section).

### A4 🟢 Feature jump/continuous HAR-CJ come INPUT · HAR-CJ jump/continuous features as INPUT — **IMPLEMENTATO 2026-07-15 (lever inerte)**
🇮🇹 Il probe semivarianza è fallito come **target** (momenti dispari); la decomposizione bipower/jump variation come **input** è ortogonale al kill: componenti C e J hanno persistence diverse (Andersen–Bollerslev–Diebold) e separarle migliora il forecast del momento pari. Causale, calcolabile dal path 1h. Effort S ma richiede rigen dataset + retrain → **accodare a un retrain già pianificato**. **Stato 2026-07-15: codice INERTE nel `FeatureBuilder`** (`features.har_cj: false` in config, default OFF = 104 feature bit-invariate): 6 colonne `bv_1d/1w`, `jump_1d/1w`, `jump_ratio_1d/1w` (BV = (π/2)·mean(|r_t||r_{t-1}|), J = max(RV−BV,0), scale TIME-semantic via `_tbars`); wiring su 01/01-update/04/04b/99; test `tests/test_har_cj.py` 7/7 (inerzia bit-identica, causalità per troncamento, bounds, jump detection). **Attivazione = rigen dataset + retrain + gate pre-registrato** (finestra GPU post-gate-v1, insieme ad A3/A8).
**EN** The semivariance probe failed as a **target** (odd moments); bipower/jump-variation decomposition as **input** is orthogonal to that kill: C and J components have different persistence and separating them improves even-moment forecasts. Causal, computable from the 1h path. Effort S but needs dataset regen + retrain → **queue behind an already-planned retrain**. **Status 2026-07-15: INERT code in the `FeatureBuilder`** (`features.har_cj: false` in config, default OFF = 104 features bit-invariant): 6 columns `bv_1d/1w`, `jump_1d/1w`, `jump_ratio_1d/1w` (BV = (π/2)·mean(|r_t||r_{t-1}|), J = max(RV−BV,0), TIME-semantic scales via `_tbars`); wired in 01/01-update/04/04b/99; tests `tests/test_har_cj.py` 7/7 (bit-identical inertia, truncation causality, bounds, jump detection). **Activation = dataset regen + retrain + pre-registered gate** (post-gate-v1 GPU window, together with A3/A8).

### A7 🟡 Risk layer greeks-aware · Greeks-aware risk layer
🇮🇹 Il `RiskManager` è delta-one (Kelly, SL ATR, CB su DD nozionale). Il book opzioni richiede: cap di **vega netta** per posizione, circuit breaker su vega-loss mark-to-market, margin simulation Deribit (inverse/portfolio margin). Necessario solo quando il sizing passa da 1 contratto fisso a Kelly-su-edge. Effort M, post-gate. **Stato: skeleton implementato 2026-07-12 (`quantsys/trading/greeks_risk.py`), NON cablato in 04b** — dettaglio in `docs/MODEL_IMPROVEMENTS.md`.
**EN** The `RiskManager` is delta-one. An options book needs: net-**vega** cap per position, mark-to-market vega-loss circuit breaker, Deribit margin simulation (inverse/portfolio margin). Needed only when sizing moves from fixed 1 contract to Kelly-on-edge. Effort M, post-gate. **Status: skeleton implemented 2026-07-12 (`quantsys/trading/greeks_risk.py`), NOT wired into 04b** — detail in `docs/MODEL_IMPROVEMENTS.md`.

### A8 🟢 Quick-win training già cablati (solo config) · Already-wired training quick-wins (config-only) — **PRE-REGISTRATO 2026-07-15 (solo mixup)**
🇮🇹 Misurabile sul giudice QLIKE dove val→test è coerente; **gate pre-registrato in STATUS.md il 2026-07-15** (overlay inerte `config/arch/itransformer_a8_mixup.yaml`, run nella finestra GPU post-gate-v1): `mixup_alpha: 0.2` — implementato in `02_train.py` (mixup temporale, lam biasato verso l'originale), oggi 0.0. Unica augmentation che preserva la coerenza cross-feature per costruzione; su target log-RV il mix di y = media geometrica delle RV (coerente). ⚠ La metà drop_path dell'item originario è **OBSOLETA** (2026-07-15): l'incumbent è GIÀ a `drop_path_rate: 0.2` dal tuning pre-registrato 2026-06-10.
**EN** Measurable on the QLIKE judge where val→test is coherent; **gate pre-registered in STATUS.md on 2026-07-15** (inert overlay `config/arch/itransformer_a8_mixup.yaml`, run in the post-gate-v1 GPU window): `mixup_alpha: 0.2` — implemented in `02_train.py` (temporal mixup, lam biased toward the original), currently 0.0; the only augmentation that preserves cross-feature consistency by construction; on the log-RV target the y mix = geometric mean of RVs (coherent). ⚠ The drop_path half of the original item is **OBSOLETE** (2026-07-15): the incumbent ALREADY uses `drop_path_rate: 0.2` from the pre-registered 2026-06-10 tuning.

### A9 🟢 Blocco MaxPool parallelo in N-HiTS (componente jump) · Parallel MaxPool block in N-HiTS (jump component) — **IMPLEMENTATO 2026-07-15 (lever inerte)**
🇮🇹 Il codice usa AvgPool (passa-basso, deviazione dal paper che usa MaxPool). Per il target di vol gli spike sono informativi (jump della RV): un blocco MaxPool in parallelo cattura la componente jump. Effort S, prior moderato, val-first sul QLIKE. **Stato 2026-07-15: codice INERTE** (`nhits_max_pool_block: false` in `config/arch/nhits.yaml`, default OFF = zero parametri nuovi, state_dict/forward bit-identici, checkpoint compatibili): blocco `NHiTSBlock(pool_type="max")` PARALLELO su `h` originale — somma il forecast latente, backcast scartato, catena residuale AvgPool INVARIATA; chiave persistita in `config.json` via `**mcfg` (parity load). Test `tests/test_nhits_maxpool.py` 10/10. **Attivazione = retrain con gate QLIKE pre-registrato** (finestra GPU post-gate-v1).
**EN** Code uses AvgPool (low-pass; paper uses MaxPool). For the vol target spikes are informative (RV jumps): a parallel MaxPool block captures the jump component. Effort S, moderate prior, val-first on QLIKE. **Status 2026-07-15: INERT code** (`nhits_max_pool_block: false` in `config/arch/nhits.yaml`, default OFF = zero new params, bit-identical state_dict/forward, compatible checkpoints): a PARALLEL `NHiTSBlock(pool_type="max")` on the original `h` — adds the latent forecast, backcast discarded, AvgPool residual chain UNCHANGED; key persisted to `config.json` via `**mcfg` (load parity). Tests `tests/test_nhits_maxpool.py` 10/10. **Activation = retrain with a pre-registered QLIKE gate** (post-gate-v1 GPU window).

### A10 🟡 Sparsity dell'attention (solo se A8 non basta) · Attention sparsity (only if A8 is insufficient)
🇮🇹 Penalità entropica sulle mappe di attention di `iTransformerLayer` (F=104 token, correlazioni spurie). ⚠ Costo: materializzare la matrice rinuncia a Flash-Attention su quel path (stabilità fp16). Effort M. Ultimo candidato training rimasto dopo A8 (drop_path obsoleto, A2/A5 chiusi).
**EN** Entropy penalty on `iTransformerLayer` attention maps. ⚠ Cost: materializing the matrix forfeits Flash-Attention on that path (fp16 stability). Effort M. Last remaining training candidate after A8 (drop_path obsolete, A2/A5 closed).

### A12 🟠 Banda di hedge gamma-scalata (Whalley–Wilmott) · Gamma-scaled hedge band (Whalley–Wilmott)
> ✅ **CODICE 2026-07-14, INERTE** — `--hedge-band-mode ww` + `--hedge-ww-lambda` in `04b` (default `fixed` = design attuale bit-identico). Attivazione: DENTRO la pre-registrazione hedged-vs-unhedged, dopo confronto offline fixed-vs-ww sul dry-run A6. · **CODE 2026-07-14, INERT** — activate INSIDE the hedged-vs-unhedged pre-registration, after the offline fixed-vs-ww comparison on the A6 dry-run.
🇮🇹 La no-trade band ottimale sotto costi proporzionali scala con Γ^(2/3) (half-width asintotica W–W 1997: `(3·k·S·Γ²/2λ)^(1/3)`). Sulle dailies il Γ ATM cresce di ordini di grandezza verso scadenza → banda fissa = churn a inizio vita E delta nudo a fine vita. Fail-soft: greeks assenti → fallback alla banda fissa; clip a [band/4, 4·band]. Effort S (fatto), λ da congelare alla pre-registrazione.
**EN** The optimal no-trade band under proportional costs scales with Γ^(2/3) (W–W 1997 asymptotic half-width `(3·k·S·Γ²/2λ)^(1/3)`). On dailies ATM Γ grows by orders of magnitude toward expiry → a fixed band churns early AND leaves naked delta late. Fail-soft: missing greeks → fixed-band fallback; clipped to [band/4, 4·band]. λ frozen at pre-registration.

### A13 🟠 Pin risk: early-close T−x + gamma cap di libro · Pin risk: T−x early-close + book gamma cap
> ✅ **CODICE 2026-07-14, INERTE** — (a) `--pin-close-hours`/`--pin-close-band` in `04b` (default OFF = hold-to-expiry pre-registrato intatto); (b) `max_net_gamma` in `GreeksLimits` (default None = nessun cap). Attivazione: pre-registrazione sizing v2, post-gate n≥20. · **CODE 2026-07-14, INERT** — activate at the v2 sizing pre-registration, post-gate n≥20.
🇮🇹 Nelle ultime ore il Γ ATM esplode e il PnL marginale è pin-risk (posizione di S vs K), non più la bet RV-vs-IV. (a) early-close quando restano ≤x ore E |S−K|/S è nella pin region: incassa, rinuncia alle ore a Sharpe peggiore; (b) cap sul Γ netto di libro (pattern `_cap_scale`) contro convessità corta concentrata a scadenza — rilevante per il braccio short-vol. Entrambi cambiano regole pre-registrate → SOLO v2.
**EN** In the final hours ATM Γ explodes and marginal PnL is pin risk (S vs K), no longer the RV-vs-IV bet. (a) early-close when ≤x hours remain AND |S−K|/S is inside the pin region; (b) net book-Γ cap (`_cap_scale` pattern) against expiry-concentrated short convexity — relevant for the short-vol arm. Both alter pre-registered rules → v2 ONLY.

### A14 🟡 Sizing vega-normalizzato · Vega-normalized sizing
> ✅ **CODICE 2026-07-14, INERTE** — `--size-mode vega` + `--size-vega-target` in `04b` (default `contracts` = 1 contratto fisso pre-registrato). Attivazione: pre-registrazione sizing v2 (con A7 nel critical path), post-gate n≥20. · **CODE 2026-07-14, INERT** — activate at the v2 sizing pre-registration (with A7 on the critical path), post-gate n≥20.
🇮🇹 A size fissa l'esposizione di vol per trade varia col tenor all'entry (~22-30h) → bet non uniformi in spazio-vol, statistica del gate sporca. Normalizzare per la vega di struttura all'entry (`amount = target_vega/Σν`, step 0.1, cap fail-safe) rende ogni trade la stessa bet. Col ladder multi-expiry ([[idea in memoria]]) diventa quasi obbligatorio: senza, i rung corti dominano il Γ di libro. Fallback fail-soft a size fissa se greeks assenti.
**EN** At fixed size, per-trade vol exposure varies with entry tenor (~22-30h) → non-uniform bets in vol space, noisier gate statistics. Normalizing by entry structure vega (`amount = target_vega/Σν`, 0.1 step, fail-safe cap) makes every trade the same bet. With the multi-expiry ladder it becomes near-mandatory: without it, short rungs dominate book Γ. Fail-soft fallback to fixed size when greeks are missing.

---

## B · Verdetto strategico: book a due strumenti (futures + opzioni) · Strategic verdict: two-instrument book

### B2 ✅ Futures come delta-hedge del book opzioni — SÌ · Futures as options-book delta-hedge — YES
🇮🇹 Il perp Deribit non è una bet: isola il PnL di volatilità. Risultato di robustezza (El Karoui / Carr–Madan), posizione delta-hedgiata:

```
PnL = ∫ ½·Γ_t·S_t²·(σ²_impl − σ²_real) dt
```

= puro harvest di (IV²−RV²), esattamente la quantità che il NN predice (+30% QLIKE vs HAR). Oggi hold-to-expiry senza hedge: PnL = premio − |S_T − K|, contaminato dal termine direzionale impredicibile. Benefici: (1) varianza per trade ↓↓ → il gate n≥20 acquista potere statistico; (2) il sospetto "edge Trending-driven" diventa testabile pulito (il termine di trend sparisce per costruzione); (3) stessa venue → margine incrociato, spread perp ~1bp. La "leva" = solo efficienza di margine, non driver di rendimento. (L'alternativa B1 — futures direzionali con leva — è KILL dimostrato: esito in `STATUS.md`.)
**EN** The Deribit perp is not a bet: it isolates vol PnL (Carr–Madan robustness result above) = pure (IV²−RV²) harvest, exactly what the NN predicts. Current unhedged hold-to-expiry PnL is contaminated by the unpredictable directional term. Benefits: per-trade variance ↓↓ (gate n≥20 gains power); the "Trending-driven edge" hypothesis becomes cleanly testable; same venue → cross-margin. "Leverage" = margin efficiency only. (The B1 alternative — leveraged directional futures — is a proven KILL: outcome in `STATUS.md`.)

🇮🇹 **Costi/caveat da mettere nel design:** hedging discreto orario → errore O(Γ·(ΔS)²) su ~30 ribilanciamenti/tenor, da misurare; funding del perp nel carry (dati funding già raccolti — riuso); opzioni Deribit inverse/coin-settled → delta in termini BTC, attenzione alla convenzione; fee perp per ribilanciamento (trascurabili a 1 contratto, da contare net al sizing vero).
**EN** **Design caveats:** hourly discrete hedging → O(Γ·(ΔS)²) error over ~30 rebalances/tenor, to be measured; perp funding in carry (funding data already collected — reuse); Deribit inverse options → BTC-terms delta convention; perp fees per rebalance.

### B3 Sequencing (vincolante) · Sequencing (binding)
🇮🇹
1. ~~ORA: A6~~ ✅ FATTO 2026-07-08 (exec-diag in `04b`; esito in `STATUS.md`).
2. **POST-GATE (chiusura v1 imminente):** `04b` v2 con leg delta-hedge sul perp testnet — pre-registrazione DRAFT già scritta (STATUS 2026-07-12), band/conv da congelare dal dry-run A6 (regola pre-dichiarata), inclusa la scelta banda fixed-vs-ww (A12). Stimatore di coda per il sizing v2: **HAR-q90** (esito A2, definitivo). Poi A7 (risk greeks-aware) + A13 (pin-close/gamma cap) + A14 (sizing vega-normalizzato) quando il sizing diventa Kelly-su-edge — pre-registrazione sizing v2 separata.
3. **Opportunistici (a retrain pianificato):** A4 (HAR-CJ input); A8 mixup quando si rigira un training comunque; A3 (mixture-of-universes) come esperimento modello a sé con gate QLIKE già pre-registrato.

**EN**
1. ~~NOW: A6~~ ✅ DONE 2026-07-08 (exec-diag in `04b`; outcome in `STATUS.md`).
2. **POST-GATE (v1 closure imminent):** `04b` v2 with a perp delta-hedge leg on testnet — DRAFT pre-registration already written (STATUS 2026-07-12), band/conv to freeze from the A6 dry-run (pre-declared rule), including the fixed-vs-ww band choice (A12). Tail estimator for v2 sizing: **HAR-q90** (A2 outcome, definitive). Then A7 (greeks-aware risk) + A13 (pin-close/gamma cap) + A14 (vega-normalized sizing) when sizing becomes Kelly-on-edge — separate v2 sizing pre-registration.
3. **Opportunistic (at a planned retrain):** A4 (HAR-CJ inputs); A8 mixup whenever a training run happens anyway; A3 (mixture-of-universes) as a standalone model experiment with the already pre-registered QLIKE gate.

🇮🇹 **Sintesi:** la parte finale del progetto diventa un book a due strumenti con i ruoli **opzioni = veicolo dell'edge (vol), futures = copertura che lo purifica** — mai il contrario.
**EN** **Summary:** the project's final stage becomes a two-instrument book where **options = edge vehicle (vol), futures = the hedge that purifies it** — never the reverse.
