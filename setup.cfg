[tool:pytest]
testpaths = tests
addopts   = -v --tb=short --timeout=120
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: test lenti (HMM training, download rete)
